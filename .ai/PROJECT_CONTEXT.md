# Project Context

Mission:
Build a reusable document privacy engine.

Architecture:
PDF/Image -> Parser -> DocumentIR -> Detectors -> Fusion -> Policy -> Renderer.

Initial users:
Developers via CLI.

Out of scope:
Web UI, cloud deployment, databases, authentication.
