# Workflow Rules

1. Work exclusively on the `dev` branch.
2. Never push directly to `main`.
3. Always create a PR from `dev` → `main` for every task.
4. Never merge a PR without explicit user approval.
5. After a force push that closes an existing PR, create a new PR.
